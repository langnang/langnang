services:
  pi-alert:
    image: jokobsk/pi.alert:latest
    container_name: pi-alert
    labels:
      yantr.app: "pi-alert"
      yantr.service: "Net Monitor"
    ports:
      - "20211/udp"
    volumes:
      - pi_alert_config:/config
    restart: unless-stopped

volumes:
  pi_alert_config:
