services:
  droppy:
    image: silverwind/droppy:latest
    container_name: droppy
    labels:
      yantr.app: "droppy"
      yantr.service: "File Manager"
    ports:
      - "8989"
    volumes:
      - droppy_config:/config
      - droppy_files:/files
    restart: unless-stopped

volumes:
  droppy_config:
  droppy_files:
