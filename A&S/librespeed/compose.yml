services:
  speedtest:
    image: ghcr.io/librespeed/speedtest:latest
    container_name: librespeed
    labels:
      yantr.app: "librespeed"
      yantr.service: "Speed Test"
    ports:
      - "8080"
    restart: unless-stopped