services:
  networking-toolbox:
    image: lissy93/networking-toolbox:latest
    container_name: networking-toolbox
    labels:
      yantr.app: "networking-toolbox"
      yantr.service: "Net Tools"
    ports:
      - "3000"
    restart: unless-stopped
