services:
  frpc:
    image: snowdreamtech/frpc
    environment:
      - DEVICE_NAME=VM-Ubuntu-24
      - DEVICE_SLUG=53
    volumes:
      - ./../../../-Linux/docker/compose/frpc/frpc.toml:/etc/frp/frpc.toml
    restart: unless-stopped
