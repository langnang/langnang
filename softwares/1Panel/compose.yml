networks:
  software-network:
    external: true
services:
  latest:
    image: moelin/1panel:latest
    ports:
      - 10086
    volumes:
      - //var/run/docker.sock:/var/run/docker.sock
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
    networks:
      - software-network
